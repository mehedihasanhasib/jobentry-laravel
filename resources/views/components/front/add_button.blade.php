<div class="">
    <button class='btn btn-primary m-3' id="{{ $id }}" data-submitroute="{{ $submitroute ?? null }}" onclick="addInformation($(this))">
        <i class="fa fa-plus"></i> Add {{ $module }}
    </button>
</div>
