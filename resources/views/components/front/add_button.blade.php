
<div class="">
    <button class='btn btn-primary' id="{{ $id ?? null }}" data-submitroute="{{ $submitroute ?? null }}" onclick="addInformation($(this))" style="color:white">
        <i class="fa fa-plus"></i> Add {{ $module ?? null }}
    </button>
</div>
